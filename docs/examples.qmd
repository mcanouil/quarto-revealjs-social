---
title: "Examples"
subtitle: "One topic at a time."
description: "A page for each group of social options, and two decks rendered with and without the filter."
---

Each page below takes one group of options and shows the YAML that sets it and the tags it produces.

| Page | What it covers |
| --- | --- |
| [Card style and image type](examples/card-style-and-image.qmd) | `card-style` and `image-type`, and what decides the default card style. |
| [Attribution](examples/attribution.qmd) | `twitter-site` and `twitter-creator`, and where Quarto writes the same two tags. |
| [Switching off](examples/switching-off.qmd) | `enabled`, the values it accepts, and what stays in the head once the filter is off. |

: The example pages. {.striped .hover tbl-colwidths="[32,68]"}

The [Reference](reference.qmd#options) stays the one place that lists every option, its type and its default.

## The two decks

This site builds two decks that carry the same social metadata:

- [with the filter](preview.html);
- [without it](preview-without.html).

Viewing the source of each, or fetching them, shows what the filter changes.

![](preview.html){.embed-art loading="lazy" title="The deck built with the filter" style="--atelier-embed-art-ratio: 1.78;"}

The pair is the plainest demonstration of the filter.
Only one of the two decks names the filter in its `filters` list.
The [Reference](reference.qmd#overlap-with-quarto) compares what each source writes.

## Source

The repository ships a short, standalone starting point you can copy: [`example.qmd`](https://github.com/mcanouil/quarto-revealjs-social/blob/main/example.qmd).
